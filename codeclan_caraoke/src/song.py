class Song:

    def __init__(self, name, genre):
        self.name = name
        self.genre = genre

    # def add_song_to_queue(self, song):
    #     if song in self.song:
    #         self.song[song] += 1

    def songs_queue(self):
        total_songs_in_queue = 0
        for song in self.songs_queue:
            total += (song + self.songs_queue)
        return total_songs_in_queue
        
